---
name: Build container image
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
      - testing
    paths-ignore:
      - '**/README.md'
  schedule:
    - cron: '17 */6 * * *'
  workflow_dispatch:

env:
  IMAGE_DESC: "My Customized Universal Blue Image"
  IMAGE_KEYWORDS: "bootc,ublue,universal-blue"
  IMAGE_LOGO_URL: "https://avatars.githubusercontent.com/u/120078124?s=200&v=4"  # Put your own image here for a fancy profile on https://artifacthub.io/!
  IMAGE_NAME: "bazzite_zone"  # output image name, usually same as repo name
  IMAGE_REGISTRY: "ghcr.io/${{ github.repository_owner }}"  # do not edit
  DEFAULT_TAG: "latest"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}-${{ inputs.brand_name}}-${{ inputs.stream_name }}
  cancel-in-progress: true

jobs:
  precheck:
    name: Check upstream base image
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: read
    outputs:
      base_digest: ${{ steps.check.outputs.base_digest }}
      should_build: ${{ steps.check.outputs.should_build }}
    steps:
      - name: Compare upstream and published image digests
        id: check
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail

          BASE_REPO="ublue-os/bazzite-deck"
          BASE_TAG="stable"
          if [[ "${GITHUB_REF}" == "refs/heads/testing" ]]; then
            BASE_TAG="testing"
          fi
          IMAGE_REPO="${GITHUB_REPOSITORY_OWNER,,}/${IMAGE_NAME,,}"
          BASE_DIGEST_LABEL="io.github.bazzite-zone.base-image-digest"

          echo "Selected upstream image: ghcr.io/${BASE_REPO}:${BASE_TAG}"

          get_registry_token() {
            local repository="$1"
            curl -fsSL \
              -u "${GITHUB_ACTOR}:${GH_TOKEN}" \
              "https://ghcr.io/token?service=ghcr.io&scope=repository:${repository}:pull" \
              | jq -er '.token'
          }

          get_manifest() {
            local repository="$1"
            local reference="$2"
            local token="$3"
            curl -fsSL \
              -H "Authorization: Bearer ${token}" \
              -H "Accept: application/vnd.oci.image.manifest.v1+json, application/vnd.docker.distribution.manifest.v2+json" \
              "https://ghcr.io/v2/${repository}/manifests/${reference}"
          }

          get_manifest_digest() {
            local repository="$1"
            local reference="$2"
            local token="$3"
            curl -fsSLI \
              -H "Authorization: Bearer ${token}" \
              -H "Accept: application/vnd.oci.image.index.v1+json, application/vnd.oci.image.manifest.v1+json, application/vnd.docker.distribution.manifest.list.v2+json, application/vnd.docker.distribution.manifest.v2+json" \
              "https://ghcr.io/v2/${repository}/manifests/${reference}" \
              | awk 'tolower($1) == "docker-content-digest:" { gsub("\\r", "", $2); print $2 }'
          }

          get_published_base_digest() {
            local token="$1"
            local manifest config_digest

            manifest="$(get_manifest "${IMAGE_REPO}" "${DEFAULT_TAG}" "${token}")"
            config_digest="$(jq -er '.config.digest' <<< "${manifest}")"

            curl -fsSL \
              -H "Authorization: Bearer ${token}" \
              "https://ghcr.io/v2/${IMAGE_REPO}/blobs/${config_digest}" \
              | jq -er --arg label "${BASE_DIGEST_LABEL}" '.config.Labels[$label]'
          }

          BASE_TOKEN="$(get_registry_token "${BASE_REPO}")"
          BASE_DIGEST="$(get_manifest_digest "${BASE_REPO}" "${BASE_TAG}" "${BASE_TOKEN}")"

          if [[ ! "${BASE_DIGEST}" =~ ^sha256:[0-9a-f]{64}$ ]]; then
            echo "Unable to resolve a valid digest for ghcr.io/${BASE_REPO}:${BASE_TAG}." >&2
            exit 1
          fi

          echo "base_digest=${BASE_DIGEST}" >> "${GITHUB_OUTPUT}"

          if [[ "${GITHUB_EVENT_NAME}" != "schedule" ]]; then
            echo "Non-scheduled event; building unconditionally."
            echo "should_build=true" >> "${GITHUB_OUTPUT}"
            exit 0
          fi

          PUBLISHED_DIGEST=""
          if IMAGE_TOKEN="$(get_registry_token "${IMAGE_REPO}" 2>/dev/null)"; then
            PUBLISHED_DIGEST="$(get_published_base_digest "${IMAGE_TOKEN}" 2>/dev/null || true)"
          fi

          if [[ "${PUBLISHED_DIGEST}" == "${BASE_DIGEST}" ]]; then
            echo "The published image already uses ${BASE_REPO}@${BASE_DIGEST}; skipping."
            echo "should_build=false" >> "${GITHUB_OUTPUT}"
          else
            echo "Upstream digest changed or no prior digest label exists; building."
            echo "should_build=true" >> "${GITHUB_OUTPUT}"
          fi

  build_push:
    name: Build and push image
    needs: precheck
    if: needs.precheck.outputs.should_build == 'true'
    runs-on: ubuntu-24.04

    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      # These stage versions are pinned by https://github.com/renovatebot/renovate
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          submodules: recursive

      - name: Prepare environment
        run: |
          # Lowercase the image uri
          echo "IMAGE_REGISTRY=${IMAGE_REGISTRY,,}" >> ${GITHUB_ENV}
          echo "IMAGE_NAME=${IMAGE_NAME,,}" >> ${GITHUB_ENV}

      - name: Prepare build labels
        run: |
          OPENZOTAC_SHA="$(git -C vendor/OpenZotacZone rev-parse HEAD)"
          ELEKTROCODER_SHA="$(git -C vendor/ElektroCoder-zotac-zone-platform rev-parse HEAD)"
          source build_files/dependencies.env
          COOLERCONTROL_URL="https://gitlab.com/coolercontrol/coolercontrol/-/releases/${COOLERCONTROL_VERSION}/downloads/packages/CoolerControlD-x86_64.AppImage"
          echo "OPENZOTAC_REV=${OPENZOTAC_SHA}" >> "${GITHUB_ENV}"
          echo "ELEKTROCODER_REV=${ELEKTROCODER_SHA}" >> "${GITHUB_ENV}"
          {
            echo 'CUSTOM_LABELS<<EOF'
            echo "io.github.bazzite-zone.repo-sha=${GITHUB_SHA}"
            echo "io.github.bazzite-zone.workflow-run-id=${GITHUB_RUN_ID}"
            echo "io.github.bazzite-zone.openzotac-sha=${OPENZOTAC_SHA}"
            echo "io.github.bazzite-zone.elektrocoder-sha=${ELEKTROCODER_SHA}"
            echo "io.github.bazzite-zone.coolercontrol-url=${COOLERCONTROL_URL}"
            echo "io.github.bazzite-zone.base-image-digest=${{ needs.precheck.outputs.base_digest }}"
            echo 'EOF'
          } >> "${GITHUB_ENV}"

      # This is optional, but if you see that your builds are way too big for the runners, you can enable this by uncommenting the following lines:
      - name: Maximize build space
        uses: ublue-os/remove-unwanted-software@695eb75bc387dbcd9685a8e72d23439d8686cba6

      - name: Get current date
        id: date
        run: |
          # This generates a timestamp like what is defined on the ArtifactHub documentation
          # E.G: 2022-02-08T15:38:15Z'
          # https://artifacthub.io/docs/topics/repositories/container-images/
          # https://linux.die.net/man/1/date
          echo "date=$(date -u +%Y\-%m\-%d\T%H\:%M\:%S\Z)" >> $GITHUB_OUTPUT

      # Image metadata for https://artifacthub.io/ - This is optional but is highly recommended so we all can get a index of all the custom images
      # The metadata by itself is not going to do anything, you choose if you want your image to be on ArtifactHub or not.
      - name: Image Metadata
        uses: docker/metadata-action@030e881283bb7a6894de51c315a6bfe6a94e05cf # v6.0.0
        id: metadata
        with:
          # This generates all the tags for your image, you can add custom tags here too!
          # Default tags are "$DEFAULT_TAG" and "$DEFAULT_TAG.$date".
          tags: |
            type=raw,value=${{ env.DEFAULT_TAG }},enable=${{ github.ref == format('refs/heads/{0}', github.event.repository.default_branch) }}
            type=raw,value=${{ env.DEFAULT_TAG }}.{{date 'YYYYMMDD'}},enable=${{ github.ref == format('refs/heads/{0}', github.event.repository.default_branch) }}
            type=raw,value={{date 'YYYYMMDD'}},enable=${{ github.ref == format('refs/heads/{0}', github.event.repository.default_branch) }}
            type=raw,value=testing,enable=${{ github.ref == 'refs/heads/testing' }}
            type=sha,enable=${{ github.event_name == 'pull_request' }}
            type=ref,event=pr
          labels: |
            io.artifacthub.package.readme-url=https://raw.githubusercontent.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/${{ github.sha }}/README.md
            org.opencontainers.image.created=${{ steps.date.outputs.date }}
            org.opencontainers.image.description=${{ env.IMAGE_DESC }}
            org.opencontainers.image.documentation=https://raw.githubusercontent.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/${{ github.sha }}/README.md
            org.opencontainers.image.source=https://github.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/blob/${{ github.sha }}/Containerfile
            org.opencontainers.image.title=${{ env.IMAGE_NAME }}
            org.opencontainers.image.url=https://github.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/tree/${{ github.sha }}
            org.opencontainers.image.vendor=${{ github.repository_owner }}
            org.opencontainers.image.version=${{ env.DEFAULT_TAG }}.{{date 'YYYYMMDD'}}
            io.artifacthub.package.deprecated=false
            io.artifacthub.package.keywords=${{ env.IMAGE_KEYWORDS }}
            io.artifacthub.package.license=Apache-2.0
            io.artifacthub.package.logo-url=${{ env.IMAGE_LOGO_URL }}
            io.artifacthub.package.prerelease=false
            containers.bootc=1
          sep-tags: " "
          sep-annotations: " "

      - name: Prepare Secure Boot signing inputs
        run: |
          if [[ "${{ github.event_name }}" != "pull_request" && "${{ github.ref }}" == "refs/heads/${{ github.event.repository.default_branch }}" ]]; then
            if [[ -z "${{ secrets.SECUREBOOT_MOK_KEY }}" || -z "${{ secrets.SECUREBOOT_MOK_CERT }}" ]]; then
              echo "SECUREBOOT_MOK_KEY and SECUREBOOT_MOK_CERT must be configured for release builds." >&2
              exit 1
            fi

            echo "SECUREBOOT_MOK_KEY_B64=$(printf '%s' '${{ secrets.SECUREBOOT_MOK_KEY }}' | base64 -w0)" >> "${GITHUB_ENV}"
            echo "SECUREBOOT_MOK_CERT_B64=$(printf '%s' '${{ secrets.SECUREBOOT_MOK_CERT }}' | base64 -w0)" >> "${GITHUB_ENV}"
          else
            echo "SECUREBOOT_MOK_KEY_B64=" >> "${GITHUB_ENV}"
            echo "SECUREBOOT_MOK_CERT_B64=" >> "${GITHUB_ENV}"
          fi

      - name: Build Image
        id: build_image
        uses: redhat-actions/buildah-build@7a95fa7ee0f02d552a32753e7414641a04307056 # v2
        with:
          containerfiles: |
            ./Containerfile
          # Postfix image name with -custom to make it a little more descriptive
          # Syntax: https://docs.github.com/en/actions/learn-github-actions/expressions#format
          image: ${{ env.IMAGE_NAME }}
          tags: ${{ steps.metadata.outputs.tags }}
          build-args: |
            BASE_IMAGE=ghcr.io/ublue-os/bazzite-deck@${{ needs.precheck.outputs.base_digest }}
            SECUREBOOT_MOK_KEY_B64=${{ env.SECUREBOOT_MOK_KEY_B64 }}
            SECUREBOOT_MOK_CERT_B64=${{ env.SECUREBOOT_MOK_CERT_B64 }}
            OPENZOTAC_REV=${{ env.OPENZOTAC_REV }}
            ELEKTROCODER_REV=${{ env.ELEKTROCODER_REV }}
          labels: |
            ${{ steps.metadata.outputs.labels }}
            ${{ env.CUSTOM_LABELS }}
          oci: false

      # Rechunk is a script that we use on Universal Blue to make sure there isnt a single huge layer when your image gets published.
      # This does not make your image faster to download, just provides better resumability and fixes a few errors.
      # Documentation for Rechunk is provided on their github repository at https://github.com/hhd-dev/rechunk
      # You can enable it by uncommenting the following lines:
      # - name: Run Rechunker
      #   id: rechunk
      #   uses: ublue-os/legacy-rechunk@a925083d9af7cb04b3e2a6e8c01bfa495f38b710 # v1.0.0
      #   with:
      #     rechunk: 'ghcr.io/ublue-os/legacy-rechunk:v1.0.0-x86_64'
      #     ref: "localhost/${{ env.IMAGE_NAME }}:${{ env.DEFAULT_TAG }}"
      #     prev-ref: "${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.DEFAULT_TAG }}"
      #     skip_compression: true
      #     version: ${{ env.CENTOS_VERSION }}
      #     labels: ${{ steps.metadata.outputs.labels }} # Rechunk strips out all the labels during build, this needs to be reapplied here with newline separator

      # This is necessary so that the podman socket can find the rechunked image on its storage
      # - name: Load in podman and tag
      #   run: |
      #     IMAGE=$(podman pull ${{ steps.rechunk.outputs.ref }})
      #     sudo rm -rf ${{ steps.rechunk.outputs.output }}
      #     for tag in ${{ steps.metadata.outputs.tags }}; do
      #       podman tag $IMAGE ${{ env.IMAGE_NAME }}:$tag
      #     done

      # These `if` statements are so that pull requests for your custom images do not make it publish any packages under your name without you knowing
      # They also check if the runner is on the default branch so that things like the merge queue (if you enable it), are going to work
      - name: Login to GitHub Container Registry
        uses: docker/login-action@4907a6ddec9925e35a0a9e82d7399ccc52663121 # v4.1.0
        if: github.event_name != 'pull_request' && (github.ref == format('refs/heads/{0}', github.event.repository.default_branch) || github.ref == 'refs/heads/testing')
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push To GHCR
        uses: redhat-actions/push-to-registry@5ed88d269cf581ea9ef6dd6806d01562096bee9c # v2
        if: github.event_name != 'pull_request' && (github.ref == format('refs/heads/{0}', github.event.repository.default_branch) || github.ref == 'refs/heads/testing')
        id: push
        env:
          REGISTRY_USER: ${{ github.actor }}
          REGISTRY_PASSWORD: ${{ github.token }}
        with:
          registry: ${{ env.IMAGE_REGISTRY }}
          image: ${{ env.IMAGE_NAME }}
          tags: ${{ steps.metadata.outputs.tags }}
          username: ${{ env.REGISTRY_USER }}
          password: ${{ env.REGISTRY_PASSWORD }}

      # This section is optional and only needs to be enabled if you plan on distributing
      # your project for others to consume. You will need to create a public and private key
      # using Cosign and save the private key as a repository secret in Github for this workflow
      # to consume. For more details, review the image signing section of the README.
      - name: Install Cosign
        uses: sigstore/cosign-installer@ba7bc0a3fef59531c69a25acd34668d6d3fe6f22 # v4.1.0
        if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
        with:
          cosign-release: 'v2.6.3'

      - name: Sign container image
        if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
        run: |
          IMAGE_FULL="${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}"
          DIGEST="${{ steps.push.outputs.digest }}"
          IMAGES=""

          for tag in ${{ steps.metadata.outputs.tags }}; do
            IMAGES+="$IMAGE_FULL:$tag@$DIGEST "
          done

          cosign sign -y --key env://COSIGN_PRIVATE_KEY $IMAGES
        env:
          COSIGN_EXPERIMENTAL: false
          COSIGN_PRIVATE_KEY: ${{ secrets.SIGNING_SECRET }}
          COSIGN_PASSWORD: ${{ secrets.COSIGN_PASSWORD }}
